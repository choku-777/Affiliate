services:
    _defaults:
        autowire: true
        autoconfigure: true
        bind:
            # 取りこぼし防止アウトボックスの基準パス
            $projectDir: '%kernel.project_dir%'

    Plugin\Affiliate\:
        resource: '../../*'
        exclude: '../../{Resource,Tests}'
